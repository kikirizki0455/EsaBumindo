/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "img-link.com"],
  },
  /* config options here */
  reactStrictMode: true,
};

export default nextConfig;
